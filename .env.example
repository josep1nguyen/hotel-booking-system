# Copy thành .env và điền giá trị thật trước khi chạy docker-compose
# JWT_SECRET phải giống nhau giữa Auth Service, Room Service và các service khác
# (dùng chung để verify token) — đây là secret dùng chung toàn hệ thống, KHÔNG commit giá trị thật lên Git.
JWT_SECRET=dev_secret_change_me
